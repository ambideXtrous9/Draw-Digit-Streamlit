params = {
        "epochs": 10,
        "learning_rate": 0.001,
        "batch_size": 64,
        "hidden_dim" : 400, 
        "dropout_prob" : 0.2,
        "loss_function": "CrossEntropyLoss",
        "optimizer": "AdamW",
    }